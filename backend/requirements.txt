# In the virtual environment, you will want to run:

pip3 install django djangorestframework django-cors-headers psycopg2 psycopg2-binary

# I'm actually not sure about the psycopg2 packages, as django should have the built-in
# database engines. That is currently found under ./backend/settings.py. 
